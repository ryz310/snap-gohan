<%= form_for(@food_group) do |f| %>
  <% if @food_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_group.errors.count, "error") %> prohibited this food_group from being saved:</h2>

      <ul>
      <% @food_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t(:field_food_group_number) %><br>
    <%= f.text_field :number, maxlength: 2 %>
  </div>
  <div class="field">
    <%= f.label t(:field_food_group) %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
