<li class="fields">
  <%= f.hidden_field :id %>
  <%= f.collection_select :food_id, Food.limit(100), :id, :subdivision, selected: f.object.food_id %>
  <%= f.text_field :amount, maxlength: 10 %>[g]
  <%= link_to_remove_fields t(:button_delete), f %>
</li>